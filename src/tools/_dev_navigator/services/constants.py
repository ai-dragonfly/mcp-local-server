# Global constants for anti-flood and defaults
MAX_PAYLOAD_BYTES = 20 * 1024  # 20 KB hard cap
DEFAULT_LIMIT = 20
MAX_LIMIT = 200

# Default per-file caps for listy operations
DEFAULT_MAX_HITS_PER_FILE = 2
