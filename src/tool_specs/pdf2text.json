{
  "type": "function",
  "function": {
    "name": "pdf2text",
    "displayName": "PDF to Text",
    "category": "documents",
    "description": "Extraction de texte depuis un PDF pour des pages données. Entrée: path (string), pages (string optionnelle) — Sortie: texte concaténé et par page.",
    "parameters": {
      "type": "object",
      "properties": {
        "path": {
          "type": "string",
          "description": "Chemin du fichier PDF (relatif au projet ou absolu)"
        },
        "pages": {
          "type": "string",
          "description": "Pages à extraire (1-based), ex: '1-3,5,10'"
        },
        "limit": {
          "type": "integer",
          "description": "Nombre maximal de pages renvoyées (parmi la sélection). Défaut: 50, max: 500",
          "minimum": 1,
          "maximum": 500,
          "default": 50
        }
      },
      "required": ["path"],
      "additionalProperties": false
    }
  }
}
